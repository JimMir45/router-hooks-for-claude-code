{"id":"edge_001","category":"edge-format","subtype":"short","prompt":"OK","expected":{"task_type":"simple","framework":"CC","accept_offline":false},"note":"极短输入(<10字符)— router 已有 fast_path,沿用上下文走 CC,task_type=simple"}
{"id":"edge_002","category":"edge-format","subtype":"long","prompt":"我现在有这样一个需求:我们的 RAG pipeline 现在用 Postgres 存 metadata,但是发现复杂 filter 条件下查询变慢,P95 已经到 2 秒了,而 embedding similarity search 这块我们用的是 pgvector 0.5,数据量大概 8000 万条,主要 filter 字段有 category(20 个值)、tag(高基数)、create_time(范围查询)、score(浮点排序)。我们考虑过分库分表,也考虑过迁到 Milvus 或者 Qdrant,但又怕 dual-write 的复杂度。你觉得我们应该怎么做?有没有一个能在我们当前架构下渐进迁移的路径?另外我也想了解一下 pgvector 0.6 以后的版本是不是有针对这种场景的新优化。","expected":{"task_type":"decision","framework":"GS","accepted_either":["GS","ECC"]},"note":"长 prompt(>200 字)+ 决策 + 调研双意图。应路 GS(决策为主)或 ECC(调研为主),不能路 SP/CC"}
{"id":"edge_003","category":"edge-format","subtype":"with-codeblock","prompt":"这个函数为啥一直返回 undefined?\n```typescript\nfunction findUser(id: number) {\n  const users = db.query('SELECT * FROM users WHERE id = ?', [id]);\n  if (users.length > 0) {\n    return users[0];\n  }\n}\n```\n输入 id=42 一直拿不到结果,但 SQL 直接跑数据库是有的","expected":{"task_type":"domain","framework":"ECC","ecc_subskill":"debug"},"note":"含代码块 + bug 诊断 → ECC-debug。验 router 在代码块输入下不被 markdown 干扰"}
{"id":"edge_004","category":"edge-format","subtype":"mixed-language","prompt":"帮我用 TDD methodology 写个 LRU cache,要求 80% coverage,先 red 再 green 最后 refactor","expected":{"task_type":"execution","framework":"SP","skill":"test-driven-development"},"note":"中英混 + 多 SP 信号词(TDD/coverage/red-green-refactor)。验 router 中英混不影响判别"}
